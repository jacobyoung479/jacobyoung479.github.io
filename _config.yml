defaults:
  - scope:
      path: ""
      type: "posts"
    values:
      layout: "post"
      author: "anonymous"
  - scope:
      path: ""
    values:
      layout: "default"

plugins:
  - jekyll-feed
  - jekyll-sitemap
  - jekyll-seo-tag
